module.exports = (function(App){
    App.Event.on('image.upload.complete',function(Image){
        //Push image to the queue for web friendly conversion
        //reset cache
        // console.log(Order)
    });

});